export const setPregame = (pregame) => ({
  type: 'SET_PREGAME',
  pregame,
});

export const setGameRoom = (room) => ({
  type: 'SET_GAME_ROOM',
  room,
});

export const setTurn = (turn) => ({
  type: 'SET_TURN',
  turn,
});
